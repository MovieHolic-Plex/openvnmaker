# Optional: absolute path to an extracted Ren'Py 8.5.3 SDK, or relative to packages/app.
# Copy to .env.local and restart the local editor. Windows builds only at present.
VNMAKER_RENPY_SDK=C:/Tools/renpy-8.5.3-sdk
